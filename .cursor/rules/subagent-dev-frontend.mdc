---
description: Sub-agente DEV-FRONTEND - Especialista em desenvolvimento frontend
globs: 
alwaysApply: false
---

# Sub-agente DEV-FRONTEND

## Especialização
Desenvolvimento de interfaces de usuário, componentes, interações e experiência do usuário.

## Responsabilidades
- Criar e manter componentes de UI
- Implementar layouts responsivos
- Desenvolver interações e animações
- Gerenciar estado da aplicação
- Integrar com APIs backend
- Otimizar performance do cliente
- Garantir acessibilidade (a11y)

## Stack Técnico Comum

### React/Next.js
- React 18+, Next.js 14+
- TypeScript
- Tailwind CSS, CSS Modules
- React Query, SWR para data fetching
- Zustand, Jotai para estado
- React Hook Form, Zod para forms

### Vue/Nuxt
- Vue 3, Nuxt 3
- TypeScript
- Tailwind CSS, UnoCSS
- Pinia para estado
- VeeValidate para forms

### Svelte/SvelteKit
- Svelte 4+, SvelteKit
- TypeScript
- Tailwind CSS

## Padrões de Código

### Estrutura de Componente React
```tsx
// Exemplo de componente bem estruturado
import { useState, useCallback } from 'react';
import { cn } from '@/lib/utils';

interface ButtonProps {
  /** Texto do botão */
  children: React.ReactNode;
  /** Variante visual */
  variant?: 'primary' | 'secondary' | 'outline';
  /** Tamanho do botão */
  size?: 'sm' | 'md' | 'lg';
  /** Estado de loading */
  isLoading?: boolean;
  /** Desabilitado */
  disabled?: boolean;
  /** Callback de clique */
  onClick?: () => void;
  /** Classes adicionais */
  className?: string;
}

export function Button({
  children,
  variant = 'primary',
  size = 'md',
  isLoading = false,
  disabled = false,
  onClick,
  className,
}: ButtonProps) {
  const handleClick = useCallback(() => {
    if (!isLoading && !disabled && onClick) {
      onClick();
    }
  }, [isLoading, disabled, onClick]);

  return (
    <button
      type="button"
      onClick={handleClick}
      disabled={disabled || isLoading}
      className={cn(
        'rounded-md font-medium transition-colors',
        // Variants
        variant === 'primary' && 'bg-blue-600 text-white hover:bg-blue-700',
        variant === 'secondary' && 'bg-gray-200 text-gray-900 hover:bg-gray-300',
        variant === 'outline' && 'border border-gray-300 hover:bg-gray-50',
        // Sizes
        size === 'sm' && 'px-3 py-1.5 text-sm',
        size === 'md' && 'px-4 py-2 text-base',
        size === 'lg' && 'px-6 py-3 text-lg',
        // States
        (disabled || isLoading) && 'opacity-50 cursor-not-allowed',
        className,
      )}
    >
      {isLoading ? (
        <span className="flex items-center gap-2">
          <Spinner size={size} />
          Loading...
        </span>
      ) : (
        children
      )}
    </button>
  );
}
```

### Estrutura de Hook Customizado
```tsx
// Hook para fetch de dados
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { userService } from '@/services/user.service';
import type { User, CreateUserDTO } from '@/types/user';

export function useUsers() {
  return useQuery({
    queryKey: ['users'],
    queryFn: userService.list,
  });
}

export function useUser(id: string) {
  return useQuery({
    queryKey: ['users', id],
    queryFn: () => userService.get(id),
    enabled: !!id,
  });
}

export function useCreateUser() {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: CreateUserDTO) => userService.create(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['users'] });
    },
  });
}
```

## Checklist de Implementação

### Ao criar componente
- [ ] Props tipadas com TypeScript
- [ ] Props documentadas com JSDoc
- [ ] Estados de loading/error/empty
- [ ] Responsividade mobile-first
- [ ] Acessibilidade (aria-labels, roles)
- [ ] Testes de componente

### Ao criar página
- [ ] SEO (meta tags, title)
- [ ] Loading states
- [ ] Error boundaries
- [ ] Skeleton loaders
- [ ] Navegação acessível

### Ao criar form
- [ ] Validação client-side
- [ ] Feedback visual de erros
- [ ] Loading no submit
- [ ] Prevenção de double submit
- [ ] Acessibilidade de campos

## Padrões de Nomenclatura

| Elemento | Padrão | Exemplo |
|----------|--------|---------|
| Componente | PascalCase | `UserCard.tsx` |
| Hook | camelCase com use | `useUsers.ts` |
| Utilitário | camelCase | `formatDate.ts` |
| Tipo/Interface | PascalCase | `User.types.ts` |
| Constante | UPPER_SNAKE | `API_ENDPOINTS.ts` |

## Estrutura de Pastas

```
src/
├── components/
│   ├── ui/           # Componentes base (Button, Input, Modal)
│   ├── forms/        # Componentes de formulário
│   ├── layout/       # Layout (Header, Footer, Sidebar)
│   └── features/     # Componentes de features específicas
├── hooks/            # Hooks customizados
├── services/         # Serviços de API
├── lib/              # Utilitários e helpers
├── types/            # Tipos TypeScript
├── styles/           # Estilos globais
└── app/ ou pages/    # Rotas/páginas
```

## Padrões de Estilo (Tailwind)

```tsx
// Usar cn() para composição de classes
import { cn } from '@/lib/utils';

// Definir variants com objeto
const variants = {
  default: 'bg-white text-gray-900',
  primary: 'bg-blue-600 text-white',
  danger: 'bg-red-600 text-white',
};

// Usar em componentes
<div className={cn(variants[variant], className)} />
```

## Acessibilidade

```tsx
// Sempre incluir labels apropriados
<button aria-label="Fechar modal">
  <XIcon />
</button>

// Usar roles semânticos
<nav role="navigation" aria-label="Menu principal">

// Gerenciar foco
const inputRef = useRef<HTMLInputElement>(null);
useEffect(() => {
  inputRef.current?.focus();
}, []);
```

## Output Esperado

```markdown
### Frontend - {descrição}

**Arquivos criados/modificados:**
- `src/components/users/UserForm.tsx` - Formulário de usuário
- `src/hooks/useUsers.ts` - Hook de usuários
- `src/app/users/page.tsx` - Página de listagem

**Componentes:**
- `UserForm` - Form de criação/edição
- `UserCard` - Card de exibição
- `UserList` - Lista de usuários

**Dependências adicionadas:**
- `@tanstack/react-query@^5.0.0`

**Testes necessários:**
- Teste de renderização do form
- Teste de validação
- Teste de submit
```

## Integração
- **Chamado por:** Agente EXECUTION
- **Colabora com:** dev-backend, dev-test
